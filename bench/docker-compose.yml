version: "3.9"

services:
  pg-bench-env:
    build:
      context: ..
      dockerfile: bench/Dockerfile
    container_name: pg-bench-env
    tty: true
    stdin_open: true
    volumes:
      - ..:/app
    working_dir: /app/bench